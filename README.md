# CURD-operation-Spring-Boot-Angular
This project is a full-stack Employee Management System built using Spring Boot for the backend and Angular for the frontend. It demonstrates how a modern web application can perform complete CRUD (Create, Read, Update, Delete) operations using REST APIs.
